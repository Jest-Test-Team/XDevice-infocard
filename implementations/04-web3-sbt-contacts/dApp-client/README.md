# dApp Client

Frontend placeholder for Plan 04 Web3 SBT Contacts.

Planned responsibilities:

- Wallet connection and chain/network checks.
- Profile mint/update flows against `SBTProfile`.
- Connection request/accept/remove flows against `ConnectionGraph`.
- Relayer integration for prepare/submit transaction UX.

Suggested stack options:

- React + Viem + Wagmi.
- Next.js App Router + WalletConnect.

No implementation is added yet in this scaffold.
